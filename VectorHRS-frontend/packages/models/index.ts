import * as ApiVerbs  from "./src/api";
import { definitions, paths, operations } from "./src/schemas";
import { instance } from "./src/instance";

export const Api  = ApiVerbs;
export { definitions, paths, operations,instance };
