import type { ResourceObject } from '../../types';
const mockType = (): any | undefined => {
  return;
};

export const _logout_logout_Resource: ResourceObject = {
  baseUrl: '/logout/logout/',
  relatedurls: ['/logout/logout/'],
  name: '_logout_logout_Resource',
  type: mockType,
  fields: {},
};
