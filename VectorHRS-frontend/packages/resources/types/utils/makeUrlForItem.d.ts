import { ResourceObject, ResourceOptions } from "../types";
export declare function makeUrlForItem(itemId: number | string | undefined, resource: ResourceObject, options?: ResourceOptions | false): string;
